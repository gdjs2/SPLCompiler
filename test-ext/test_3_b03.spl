struct Complex
{
    int real;
    int imaginary;
};

int main()
{
    struct Complex c1;
    c1.real = read();
    c1.imaginary = read();
    if(c1.real==0){
        // pure imaginary
        write(c1.imaginary);
    }else{
        // otherwise
        write(c1.real);
        write(c1.imaginary);
    }
    return 0;
}
